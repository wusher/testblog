%h1 listing posts 
%table
	%tr
		%th name
		%th title
		%th content 
		%th
		%th
		%th
	- @posts.each do |post|
		%tr
			%td= post.name 
			%td= post.title 
			%td= post.content 
			%td= link_to 'show', post
			%td= link_to 'edit', edit_post_path(post)
			%td= link_to 'kill', post, :confirm => 'r u sure?', :method => :delete
%br
= link_to 'new post', new_post_path 
